{
  "name": "acuo",
  "version": "1.0.0",
  "description": "Sync process for grabbing survey information and responses from Survey Monkey and storing them into a Postgresql database for PowerBI",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Christopher Daniel Jativa",
  "license": "ISC",
  "devDependencies": {
    "@types/dotenv": "^6.1.1",
    "dotenv": "^8.0.0"
  },
  "dependencies": {
    "axios": "^0.19.0"
  }
}
